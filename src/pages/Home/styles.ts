import styled from 'styled-components'

export const Main = styled.main`
  max-width: 1200px;
  margin: 0 auto;
`

export const BgImage = styled.img`
  position: absolute;
  top: 0;
  right: 0;
  z-index: -1;
`
